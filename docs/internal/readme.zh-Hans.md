# Jant - 产品设计文档

> **Jant** = Jantelagen（詹代法则）缩写
> 强调低调、去社交化的个人表达

## 1. 产品定位

**一句话**：像 Threads.net 一样丝滑的个人微博客系统。

**核心特征**：

- 单博客，单作者
- 多种内容类型
- Thread（帖子串）支持
- Collection 策展
- 极简部署

**非目标**：

- 没有社交功能（不关注、不点赞、不转发）
- 不支持多用户
- 不做复杂权限系统

---

## 2. 内容模型

### 2.1 内容类型

| 类型        | 用途        | 示例           |
| ----------- | ----------- | -------------- |
| **note**    | 短想法      | "今天天气不错" |
| **article** | 长文        | 博客文章       |
| **link**    | 外链 + 评论 | 分享好文       |
| **quote**   | 引用他人    | 名言警句       |
| **image**   | 图片 + 说明 | 摄影作品       |
| **page**    | 独立页面    | About, Now     |

### 2.2 可见性

| 级别         | 含义           | 使用场景           |
| ------------ | -------------- | ------------------ |
| **featured** | 精选，高调展示 | 精心创作的内容     |
| **quiet**    | 正常发布       | 日常碎碎念（默认） |
| **unlisted** | 仅链接可访问   | 不想被发现的内容   |
| **draft**    | 草稿           | 还在写             |

**设计理念**：默认 quiet，手动 opt-in featured。减少发布焦虑。

### 2.3 Thread（帖子串）

**场景**：写着写着内容变长，想拆成多条但保持连贯。

**交互**：

- 发布时可选择「回复」某条帖子
- 回复会形成链式结构，用竖线连接
- 首页展示时，新帖会带上下文提示

**规则**：

- Thread 内所有帖子继承 root 的 visibility
- 删除 root = 整个 Thread 软删除
- 删除中间帖 = 链断开，但子帖保留

### 2.4 Collection（策展）

**场景**：把零散帖子组织成主题集合。

**示例**：

- `/c/reading-2024` - 2024 读书笔记
- `/c/recipes` - 食谱合集
- `/c/thoughts-on-ai` - AI 思考系列

---

## 3. UI/UX 设计

### 3.1 设计理念

**参考**：Threads.net 的丝滑 + 传统博客的内容深度

**关键词**：

- **极简**：大量留白，内容为王
- **流畅**：所有状态变化都有动画
- **移动优先**：单栏布局，桌面端最大 680px
- **即时反馈**：骨架屏、乐观更新

### 3.2 视觉规格

**间距**：宽松，给内容呼吸空间
**圆角**：柔和，8-16px
**边框**：极淡或无，用间距和阴影区分
**字体**：系统字体，15px 基准
**颜色**：单色主调，仅交互元素用强调色

### 3.3 帖子卡片设计

```
┌─────────────────────────────────────────────┐
│  [头像]  作者名 · 3小时前                    │
│    │                                        │
│    │     帖子正文内容...                     │
│    │                                        │
│    │     [图片/媒体区域]                     │
│    │                                        │
│    │     💬 回复                            │
│    │                                        │
│  (竖线连接 Thread)                          │
└─────────────────────────────────────────────┘
```

### 3.4 发布框设计

```
┌─────────────────────────────────────────────┐
│  有什么想说的...                             │
│                                             │
├─────────────────────────────────────────────┤
│  [📷] [🔗] [❝]      ☐ 精选    [发布]        │
└─────────────────────────────────────────────┘

- 默认以 quiet 发布
- 勾选「精选」则变为 featured
- 图标：添加图片、添加链接、添加引用
```

### 3.5 动画规格

- **时长**：150-300ms
- **缓动**：ease-out
- **场景**：
  - 页面切换：淡入 + 轻微上移
  - 新内容加载：骨架屏闪烁
  - 按钮点击：轻微缩放
  - 展开/收起：高度动画

---

## 4. 信息架构

### 4.1 URL 设计原则

> URL 是产品的一部分。应该简洁、美观、有意义。

### 4.2 核心路由

| URL         | 内容                             |
| ----------- | -------------------------------- |
| `/`         | 首页，所有 featured + quiet 帖子 |
| `/featured` | 仅精选帖子                       |
| `/p/{id}`   | 单条帖子                         |
| `/p/{slug}` | 单条帖子（自定义 slug）          |
| `/{slug}`   | 独立页面（About, Now 等）        |
| `/c/{slug}` | Collection                       |
| `/archive`  | 归档                             |
| `/search`   | 搜索                             |
| `/feed`     | RSS（仅 featured）               |

### 4.3 后台路由

| URL                 | 功能            |
| ------------------- | --------------- |
| `/dash`             | 仪表盘          |
| `/dash/posts`       | 帖子管理        |
| `/dash/collections` | Collection 管理 |
| `/dash/settings`    | 设置            |

---

## 5. RSS 订阅

**两个 feed**：

- `/feed` - 仅 featured，适合订阅精选内容
- `/feed/all` - featured + quiet，不想错过任何东西

**Link 类型处理**：标题加 `↗` 前缀表示外链

**Thread 处理**：非 root 帖子在内容末尾附加上下文链接

---

## 6. 主题系统

### 6.1 颜色主题

预设：default, ocean, forest, sunset, lavender, rose, sand, slate, gameboy, terminal, notepad, nord, dracula, solarized

**切换**：后台设置，自动支持 light/dark mode

### 6.2 组件覆盖

用户可在自己的项目中覆盖任意组件，实现完全定制。

---

## 7. 技术约束

- **部署目标**：Cloudflare Workers（Phase 1 仅支持这个）
- **样式**：BaseCoat UI + 有限的 Tailwind
- **交互**：Datastar（Hypermedia）
- **语义标记**：microformats2

---

## 8. Phase 1 MVP 范围

**包含**：

- 5 种内容类型（note, article, link, quote, image）+ page
- 可见性系统
- Thread 支持
- Collection 策展
- 搜索（全文）
- RSS
- 管理后台
- 多语言（en/zh）
- 颜色主题

**不包含**：

- 评论系统
- 统计分析
- 视频/音频类型
- API Token
- 自定义 CSS/JS 注入
