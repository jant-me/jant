# 挣扎

photo set 是什么类型？我当然支持多张图片呀。

分类维度，我目前倾向于只支持： collection, 其他两个相当于是系统自带的： domain 和 type

站内/外链区分, 倾向于用一个标准的格式，因为rss也会推送，所以这个标志可能直接在标题里显示

featured 机制： 我倾向于单独一个维度。

长文实现: 不知道 threads 是如何实现的？主要是如果用threads的展开讲讲，rss的标题是啥呢？

Like? 你觉得需要吗？我倾向于不加，我的项目叫 jant, 是 jantelagen 的缩写。不想太多社交，甚至 评论我在考虑是否要添加。

注册验证码： 添加 cloudflare的验证码，好添加吗？这个需要 api key什么的吗？是不是只支持 oauth 登录会好一点。

要评论，是否支持嵌套回复？ -> 不支持，就像 twitter或者 threads那样就好了。

可见性：

- public: 列表可见 + RSS
- unfeed: 列表可见 + RSS 不可见
- unlisted: 列表不可见 + RSS不可见
- private: 仅登录可见
- draft: 草稿

RSS 标题： 无标题就不应该添加标题

网页标题： 截取前面的字符？

短网址，我希望是 /p/xxxxx 最短的方案是什么？不太容易重复的。同时允许 自定义 slug 作为别名。

page 必须指定 url可以吗？

发布入口 我纠结是直接 首页，还是搞一个 new?

我觉得另外要允许 /space/x/ 或者直接用户名？ /username/ 这样去发？首页自动读取当前space，其实不应该叫 space，应该叫 blog

collection 的网址是不是也要能自定义啊？ 比如 /thoughts /quotes 之类的？或者我的问题是如何解决用户直接的网址迁移问题？

管理入口有没有更现代的，感觉 /admin/* 有点太土了？

部署模式，反正都支持注册了，那就多用户啊，第一个注册的用户自动是管理员。只是一个权限模型而已吧？

域名策略，暂时不考虑备案问题，直接子域名吧，但是子域名不提供搜索引擎索引，这样会有问题吗？（会对我的主域名有什么名誉问题吗）

免费限制： 50篇文章+不允许自定义域名, 图片限制不知道怎么弄，会不会太复杂，我想弄一个简单的限制。

关于搜索： CF Workers 不支持全文搜索吗？

多数据库： Drizzle

i18n： 还没想好，主要语言是英文，暂时只支持中文就好了。

登录方式： 邮箱，google oauth, github oauth., 还需要用户的头像，用户启动系统的时候，必须配置一个，否则就不行。

API：

- REST 为主
- 考虑支持 Micropub 标准 (IndieWeb 兼容)

导入：

- 直接开放api，用户可以自己直接调用 api 写入帖子。

搜索我肯定想内置，如果 sqlite 全文搜索不好实现的话， page find 这个静态搜索容易接入吗？
