# Jant - 产品设计文档

> **Jant** = Jantelagen（詹代法则）缩写
> 强调低调、去社交化的个人表达

## 一、产品定位

**一句话描述**：像 Threads.net 一样丝滑的个人微博客系统。

**核心特点**：
- 单博客、单博主
- 多种内容类型
- Thread（帖子串）支持
- Collection 策展
- 极简部署

**不做什么**：
- 不做社交（无关注、无点赞、无转发）
- 不做多用户
- 不做复杂的权限系统

---

## 二、内容模型

### 2.1 内容类型

| 类型 | 用途 | 示例 |
|------|------|------|
| **note** | 短想法 | "今天天气真好" |
| **article** | 长文章 | 博客文章 |
| **link** | 外部链接+评论 | 分享一篇好文章 |
| **quote** | 引用他人的话 | 名人名言 |
| **image** | 图片+说明 | 摄影作品 |
| **page** | 独立页面 | About、Now |

### 2.2 可见性

| 级别 | 含义 | 使用场景 |
|------|------|----------|
| **featured** | 精选，重点展示 | 精心写的内容 |
| **quiet** | 安静，正常发布 | 日常碎碎念（默认） |
| **unlisted** | 未列出，仅链接可访问 | 不想被发现的内容 |
| **draft** | 草稿 | 写到一半 |

**设计原则**：默认 quiet，手动勾选才是 featured。降低发布心理压力。

### 2.3 Thread（帖子串）

**场景**：一个想法写着写着变长了，想分成多条发布，但又想保持连贯性。

**交互**：
- 发布时可以选择「回复」某条帖子
- 回复会形成一条竖线连接的帖子串
- 首页显示时，新帖子会带上上下文提示

**规则**：
- Thread 内所有帖子继承根帖的可见性
- 删除根帖 = 删除整个 Thread
- 删除中间帖子 = 断链，但子帖保留

### 2.4 Collection（策展）

**场景**：把散落的帖子整理成专题。

**示例**：
- `/c/reading-2024` - 2024 年读书笔记
- `/c/recipes` - 菜谱收藏
- `/c/thoughts-on-ai` - 关于 AI 的思考

---

## 三、UI/UX 设计

### 3.1 设计理念

**参考**：Threads.net 的丝滑感 + 传统博客的内容深度

**关键词**：
- **简洁**：大量留白，内容为王
- **流畅**：所有状态变化都有动画
- **移动优先**：单栏布局，桌面端不超过 680px
- **即时反馈**：骨架屏、乐观更新

### 3.2 视觉规范

**间距**：宽松，给内容呼吸空间
**圆角**：柔和，8-16px
**边框**：极淡或无，靠间距和阴影区分
**字体**：系统字体，15px 基准
**颜色**：单色调为主，强调色仅用于交互

### 3.3 帖子卡片设计
```
┌─────────────────────────────────────────────┐
│  [头像]  作者名 · 3小时前                    │
│    │                                        │
│    │     帖子内容在这里...                  │
│    │                                        │
│    │     [图片/媒体区域]                    │
│    │                                        │
│    │     💬 回复                            │
│    │                                        │
│  (竖线，用于 Thread 连接)                   │
└─────────────────────────────────────────────┘
```

### 3.4 发布框设计
```
┌─────────────────────────────────────────────┐
│  写点什么...                                │
│                                             │
├─────────────────────────────────────────────┤
│  [📷] [🔗] [❝]      ☐ 精选      [发布]     │
└─────────────────────────────────────────────┘

- 默认发布为 quiet
- 勾选「精选」变为 featured
- 图标分别是：添加图片、添加链接、添加引用
```

### 3.5 动画规范

- **时长**：150-300ms
- **缓动**：ease-out
- **场景**：
  - 页面切换：淡入 + 轻微上移
  - 新内容加载：骨架屏闪烁
  - 按钮点击：轻微缩放
  - 展开/收起：高度动画

---

## 四、信息架构

### 4.1 URL 设计原则

> URL 是产品的一部分，应该简洁、美观、有意义。

### 4.2 核心路由

| URL | 内容 |
|-----|------|
| `/` | 首页，所有 featured + quiet 帖子 |
| `/featured` | 仅精选帖子 |
| `/p/{id}` | 单篇帖子 |
| `/p/{slug}` | 单篇帖子（有自定义 slug） |
| `/{slug}` | 独立页面（About、Now 等） |
| `/c/{slug}` | Collection |
| `/archive` | 归档 |
| `/search` | 搜索 |
| `/feed` | RSS（仅 featured） |

### 4.3 后台路由

| URL | 功能 |
|-----|------|
| `/dash` | 仪表盘 |
| `/dash/posts` | 帖子管理 |
| `/dash/collections` | Collection 管理 |
| `/dash/settings` | 设置 |

---

## 五、RSS 订阅

**两个 feed**：
- `/feed` - 仅 featured，适合订阅精选内容
- `/feed/all` - featured + quiet，适合不想错过任何内容

**Link 类型处理**：标题前加 `↗` 标识外链

**Thread 处理**：非首帖在内容末尾追加上下文链接

---

## 六、主题系统

### 6.1 颜色主题

预设主题：default, ocean, forest, sunset, lavender, rose, sand, slate, gameboy, terminal, notepad, nord, dracula, solarized

**切换方式**：后台设置，自动支持 light/dark mode

### 6.2 组件覆盖

用户可以在自己的项目中覆盖任意组件，实现完全自定义。

---

## 七、技术约束

- **部署目标**：Cloudflare Workers（一期唯一目标）
- **样式方案**：BaseCoat UI + 有限 Tailwind
- **交互方案**：Datastar（Hypermedia）
- **语义标记**：microformats2

---

## 八、一期 MVP 范围

**包含**：
- 5 种内容类型（note, article, link, quote, image）+ page
- 可见性系统
- Thread 支持
- Collection 策展
- 搜索（全文）
- RSS
- 管理后台
- 多语言（en/zh）
- 颜色主题

**不包含**：
- 评论系统
- 访问统计
- 视频/音频类型
- API Token
- 自定义 CSS/JS 注入
