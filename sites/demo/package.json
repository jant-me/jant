{
  "name": "jant-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "jant migrate --local && node scripts/dev.mjs",
    "deploy": "jant deploy",
    "reset-password": "jant reset-password"
  },
  "dependencies": {
    "@jant/core": "workspace:*"
  },
  "devDependencies": {
    "wrangler": "catalog:"
  }
}
