# 1.0 冻结准备

1.0 之后，下面这些面上的破坏性改动都要等 2.0。趁现在删改还不算破坏，把要冻结的东西定下来、
清理干净，再发版。

分两类：**A 类**有公认做法，我直接定；**B 类**是产品或对外决策，要和 Owen 商量。
B 类定完之前，A 类里依赖它的项不动。

## 已核实的前提

- 用户站点只用 `createApp()`：`sites/*`、create-jant 模板、`jant-starter`、`owen-jant`
  全是 `import { createApp } from "@jant/core"`。jant-cloud 不 import 这个包，只通过
  `/api/internal/sites/*` 调用 core。
- 0.7.0 之后公开 API 只有新增（`createdAt`/`updatedAt`、媒体列表 `cursor`、
  `durationSeconds`、导航链接 `description`、`redirectType` 接受数字、`/readyz` 的
  `version`），没有破坏性改动。
- `/api/upload/multipart` 在文档里叫 legacy，但 Jant 自己的客户端
  （`src/client/multipart-upload.ts`）还在用。
- Postgres 在 `docs/deployment-docker.md` 里是正式选项，但 CI 只演练 D1 的 migration。
- `@jant/core@1.0.0` 已在 npm 上（2026-04-19 误发，已 deprecate），版本号不能再用。
  远端有 `v1.0.0`、`@jant/core@1.0.0`、`create-jant@1.0.0` 三个旧 tag。

## A 类：我来定

### 兼容性承诺

- [ ] 新增 `docs/compatibility.md`（中英）：冻结哪些面、不冻结哪些面；弃用政策
      （minor 里标 deprecated 并在运行时提示，下一个 major 删除）；只修最新 1.x；
      不支持降级，回退靠备份；默认值变化算 minor，必须写 Upgrade notes
- [ ] `AGENTS.md` 的 pre-1.0 规则改成冻结后的规则（破坏性改动需要迁移路径和弃用期）；
      README、overview、FAQ、RELEASING.md 的 Pre-1.0 措辞在 1.0 发版时一起改

### JS 包

- [x] `src/index.ts` 只导出 `createApp`（和它的类型）。其余约 40 个类型、view helper、
      `defaultFeedRenderer`、`time`/`url`/`markdown` 命名空间都没有外部使用者，
      `createApp()` 也不接受参数，没有可接入的扩展点
- [x] `./node` 从 `exports` 移除（CLI 按文件路径加载 `dist/node.js`）；`./i18n` 保留，标为内部，
      写进兼容性文档
- [x] 模板 `wrangler.toml` 引用的 `node_modules/@jant/core/dist/client` 和
      `src/db/migrations` 列入冻结面。`check-template` 已经用真实 tarball 安装后验证这两个路径，
      不另加测试

### CLI

- [x] 删除重复别名：顶层 `jant export`（= `db export`）、`jant import-site`（= `site import`）。
      模板里名不副实的 `"export": "jant export"` 脚本直接删掉（`jant-starter` 由 CI 从模板同步）
- [x] 命名统一为「名词 动词」：`search-reindex` → `search reindex`
- [x] 分层（`bin/lib/command-registry.js`）。公开：`migrate`、`deploy`、`start`、`setup`、`reset-password`、`db export`、
      `site export|import|pull-media`、`site snapshot export|import`、
      `telegram register-webhooks`、`uploads cleanup`、`search reindex`、
      `posts rebuild-html`。内部（不进 `--help`，不受 semver 约束）：
      `assets prepare|upload`、`db execute-file`、`db rehearse`。
      `migrate-text-attachments` 见 B4
- [x] 参数统一：`--url` 在所有命令里都表示「哪个站点」（`site export|import` 的位置参数、
      `telegram register-webhooks --base-url` 改过来）；`deploy --site-path-prefix` → `--path-prefix`；
      `site export --directory` 并进 `--output <dir|zip>`。旧写法报错并给出替换写法
- [x] 新增 `docs/cli.md`（中英），`cli-commands.test.ts`：每个命令文件都已归类，公开命令的
      `--help` 用自己的名字，两份文档列出的命令和选项与 `--help` 一致

### HTTP API

- [x] ~~删除 `hasMedia`/`hasTitle`/`hasReplies`~~ 改为保留：API、公开页面、feed 订阅地址和已存的
      archive 自定义路径共用 `lib/filter-dimensions.ts` 的解析器，分享 URL 的旧参数按 AGENTS.md
      永久兼容。API 单独拒绝反而要加代码。`API.md` 不再称其 Deprecated（那意味着下个大版本删除），
      改为「为旧链接永久接受的旧写法」
- [x] ~~删除 `deletedOrphanMedia: 0`~~ 不做：它在 `/api/internal/*` 的返回里，不属于公开契约，
      而且 jant-cloud 的 zod schema 把它写成必填，要删得先改 jant-cloud
- [x] `redirectType`：文档以数字为规范形式，字符串继续接受。设置页表单（Datastar select）只产生
      字符串，API 与表单共用一个 schema，只为 API 拆 schema 不值得
- [x] `/api/upload/multipart`：客户端早已改走 `/api/uploads` 会话，`src/client/multipart-upload.ts`
      没有任何导入方。删除路由、测试、死代码和 API.md 章节；0.8.0 Upgrade notes 要写
- [ ] `/api/internal/*` 明确不属于公开契约（它是 core 与 jant-cloud 之间的约定）
- [ ] MCP 工具名和参数纳入冻结面
- [ ] 保持 `latest_hidden`（API）与 `hidden`（URL）两种写法，按现有 URL 参数规则写进兼容性文档

### 配置

- [x] 未文档化的 env 定性：`CORS_ORIGINS`、`DEFAULT_THEME`、`DEFAULT_FONT_THEME`、
      `RATE_LIMIT_DISABLED`、`RATE_LIMIT_SEARCH_PER_MIN`、`INTERNAL_ADMIN_TOKEN`、`GITHUB_APP_*`
      写进文档；`SITE_RESOLUTION_MODE`、`HOSTED_CONTROL_PLANE_*` 注明为托管服务专用。
      （`S3_FORCE_PATH_STYLE` 并不存在：路径风格由 endpoint 推断）
- [x] 修文档漂移：`CJK_SERIF_FONT`（代码已删，3 份文档还在）；保留路径列表缺
      `subscribe`、`skill.md`
- [x] 加测试 `config-docs.test.ts`：`Bindings` 里每个变量都已归类；公开变量都在 configuration.md；
      两份 configuration.md 不列代码不读的变量、保留路径与 `RESERVED_PATHS` 一致；API.md 的
      可编辑设置表与 `editableSettingKeys` 一致
- [ ] 中文 configuration.md 落后于英文版（缺 Discover、Archive layout、Telegram 等节），另开任务补齐

### 主题

- [ ] 公开面 = `docs/theming.md` 列出的 58 个变量、`data-*` 属性和文档化的 class；
      其余约 210 个变量明确为内部。补上缺的读者页面 `data-page` 值（`subscribe`）
- [ ] 加测试：`theming.md` 列出的每个变量和 `data-page` 值在代码里都存在

### Feed

- [ ] `docs/internal/feed-reading.md` 移到公开文档；`jant:post`、`jant:thread`、
      `jant:truncated`、`jant:page` 文档化并冻结。命名空间 URI 不加版本号，只做增量扩展

### 导出与 snapshot

- [ ] 导入检查 `data/jant.toml` 的 `version`：比自己新的拒绝并提示先升级
- [ ] snapshot `meta.json` 记录 Jant 版本和最后一个 migration；目标站点 schema 比
      snapshot 旧时拒绝并提示先升级
- [ ] 每次发版固定一份 snapshot 和一份 site export 作为测试夹具，CI 用当前代码导入全部
      历史夹具（扩展 `snapshot-canonical-replay.test.ts`）。这是「1.x 的备份能在之后的
      1.x 上恢复」的可执行保证
- [ ] 导出的 Hugo 主题：文档写明哪些 layout 可以覆盖，partial 属于内部

### 数据库

- [ ] Postgres migration 演练加进 CI
- [ ] 文档写明：从哪个 0.x 起可以直接升到 1.x、可以跨版本升级、不支持降级

## B 类：要商量

- [x] B1 发布顺序：先把 main 发成 0.7.1，还是做完冻结准备再发
- [x] B2 1.0 的版本号（1.0.1 / 1.1.0）；三个旧 tag 删不删
- [x] B3 Docker 镜像名 `owenyoung/jant` 是否在 1.0 前迁到组织名下
- [x] B4 旧数据迁移代码的去留：`migrate-text-attachments`、
      `/api/internal/text-attachments/migrate-envelopes`、`services/media.ts` 的旧格式读取、
      `styles/ui.css` 的 v1 sidenote CSS。取决于所有站点是否都已迁完
- [x] B5 以后新增的系统路由与用户 slug 冲突的政策
- [x] B6 是否加 `/api/v1` 前缀
- [x] B7 模板里的 `AGENTS.md` 和 `.agents/skills/*` 会在用户仓库里过期，是否改成指向站点
      `/skill.md` 的薄文件

## B 类决定的落实

- [x] B4：删除旧文本附件转换和 v1 sidenote CSS（`56932bdc`）
- [x] B7：模板只留薄 `AGENTS.md`，删除 `.agents/skills/` 和生成的 `.claude/skills/`
- [ ] B3：`docs/RELEASING.md` 写明镜像名固定为 `owenyoung/jant`
- [ ] B5：兼容性文档写明新根路径与用户地址冲突时的 semver 规则

## 发版

- [x] 0.7.1 changeset（`30bb9b68`，已推送；CI 开 Release PR，合并即发布）
- [ ] 0.7.1 发布后：确认 npm 和 Docker Hub 上的 0.7.1 能导入 canonical demo 导出
- [ ] 冻结准备全部完成 → 0.8.0（Upgrade notes 覆盖本文件里所有删除和改名）
- [ ] 0.8.x 在 hosted 和 owenyoung.com 上稳定运行几周，期间不改 schema → 1.0.1

## 决定记录

2026-09-26 与 Owen 确认：

- B1：现在把 main 发成 0.7.1（只含 0.7.0 以来的新增和修复）；冻结准备另发 0.8.0，
  跑几周后发 1.0
- B2：1.0 用 1.0.1，changelog 说明 1.0.0 是误发；旧 tag 保留
- B3：镜像名固定为 `owenyoung/jant`。`docs/RELEASING.md` 里「owner 可能会变」的说法改掉
- B4：在 0.8.0 里删除 `migrate-text-attachments`、
  `/api/internal/text-attachments/migrate-envelopes`、`services/media.ts` 的旧格式转换代码、
  `styles/ui.css` 的 v1 sidenote CSS。Upgrade notes 写明：4 月 16 日之前建站并用过文本附件的，
  先在 0.7.x 上跑一次 `jant migrate-text-attachments` 再升级。owenyoung.com 已转换
  （演练中 19 个文本附件完整导出）
- B5：不预先决定哪一方优先。兼容性文档只写 semver 的后果：新根路径如果会占掉用户已有的
  地址，只能在 major 版本里加；minor 里加的根路径必须让用户已有内容优先
- B6：不加 `/api/v1`
- B7：模板只留一个指向站点 `/skill.md` 的薄 `AGENTS.md`，删掉复制进去的 skill 文件
