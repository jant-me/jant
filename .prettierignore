# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
.wrangler/
.vite/
# Generated by `vite build --config vite.config.site.ts` (build:site); committed
# minified so the worker build can ?raw-inline them. Keep prettier off so the
# committed bytes match the build output instead of getting re-expanded on commit.
packages/core/src/services/export-theme/assets/
# Hugo layout templates use Go template expressions inside HTML attributes,
# which Prettier's HTML parser cannot parse safely.
packages/core/src/services/export-theme/layouts/
sites/demo-source/canonical/site-export/themes/jant/layouts/
# Generated demo site-export client bundle.
sites/demo-source/canonical/site-export/themes/jant/static/_jant/

# Generated files
**/assets.gen.ts
**/i18n/locales/*.ts
*.po
*.pot
pnpm-lock.yaml

# Vendored upstream bundles, kept byte-identical to the reference copy under
# `references/datastar/` so a version bump reads as a clean diff. Prettier's own
# formatting drift between releases (`for (; n; )` vs `for (; n;)`) would
# otherwise rewrite them on every upgrade.
packages/core/src/vendor/

# Config
.lingui/
references/
packages/create-jant/template/

# Assets
**/src/assets/
sites/demo/static-site-export/templates/

# Logs
*.log
