# =============================================================================
# Tools
# =============================================================================
[tools]
node = "lts"
pnpm = "10.28.2"
hugo-extended = "0.160.1"

# =============================================================================
# Dev (packages/core dev environment)
# =============================================================================
[tasks.dev]
description = "Start development server (Wrangler/Miniflare with Vite HMR)"
run = "pnpm --filter @jant/core i18n:build && pnpm --filter @jant/core dev"

[tasks.dev-node]
description = "Start the Node runtime with Vite-managed HMR"
# Node fetch only reads HTTP_PROXY/HTTPS_PROXY/NO_PROXY when this startup
# opt-in is present. The Vite config runs too late to enable it retroactively.
env = { NODE_USE_ENV_PROXY = "1" }
run = "pnpm --filter @jant/core i18n:build && pnpm --filter @jant/core dev:node"

[tasks.dev-debug]
description = "Start debug server on the first free port starting at 19020 with local auth helpers ready"
run = "pnpm --filter @jant/core dev:debug"

[tasks.dev-auth-password]
description = "Set the local admin password used for browser/agent debugging"
usage = 'arg "[password]" default="jant-dev-debug-login"'
dir = "packages/core"
run = 'node dev/scripts/set-dev-password.mjs "$usage_password"'

[tasks.dev-auth-bootstrap]
description = "Configure local demo login helpers for browser/agent debugging"
usage = 'arg "[password]" default="jant-dev-debug-login"'
dir = "packages/core"
run = 'node dev/scripts/setup-dev-auth.mjs "$usage_password"'

[tasks.dev-auth-sync]
description = "Update the local Node admin password to match DEMO_PASSWORD in .env.node (or arg)"
usage = 'arg "[password]"'
dir = "packages/core"
run = 'pnpm exec tsx dev/scripts/sync-dev-password.ts "$usage_password"'

[tasks.serve-node]
description = "Serve the production Node runtime locally (reads packages/core/.env.node)"
run = "HOST=0.0.0.0 node --env-file packages/core/.env.node packages/core/bin/jant.js start"

# =============================================================================
# Build
# =============================================================================
[tasks.build]
description = "Build @jant/core (lib + client assets)"
run = "pnpm --filter @jant/core build"

[tasks.build-lib]
description = "Build @jant/core library for publishing"
run = "pnpm --filter @jant/core build:lib"

[tasks.build-packages]
description = "Build all publishable packages"
run = """
pnpm --filter @jant/core build
pnpm --filter create-jant build
"""

[tasks.build-create]
description = "Build create-jant CLI"
run = "pnpm --filter create-jant build"

[tasks.dev-create]
description = "Watch and rebuild create-jant CLI"
run = "pnpm --filter create-jant dev"

[tasks.upload-assets]
description = "Upload built static assets to S3-compatible storage (reads packages/core/.env.node)"
run = "node --env-file packages/core/.env.node packages/core/bin/jant.js assets upload"

[tasks.brand-export]
description = "Export the default Jant brand assets to a local directory"
usage = 'arg "[path]" default="packages/core/src/assets/branding/generated"'
run = 'pnpm exec tsx packages/core/dev/scripts/generate-brand-assets.ts --export-dir "$usage_path" --export-only'

[tasks.jant]
description = "Run the local Jant CLI from packages/core/bin/jant.js"
usage = 'arg "[args]" var=#true double_dash="automatic"'
run = """
#!/usr/bin/env bash
set -euo pipefail

args=()
if [[ -n "${usage_args:-}" ]]; then
  eval "args=($usage_args)"
fi

exec ./packages/core/bin/jant.js "${args[@]}"
"""

# =============================================================================
# Testing
# =============================================================================
[tasks.test]
alias="check-tests"
description = "Run all tests (typecheck + lint + vitest)"
run = """
mise run check-types
mise run check-lint
# CLI snapshot tests import dist/node.js via loadNodeRuntime().
pnpm --filter @jant/core build:lib
pnpm --filter @jant/core test
"""

[tasks.check-tests-watch]
description = "Run tests in watch mode"
run = "pnpm --filter @jant/core test:watch"

[tasks.check-tests-coverage]
description = "Run tests with coverage report"
run = "pnpm --filter @jant/core test:coverage"

[tasks.check-pg-smoke]
description = "Run the Postgres smoke test against PG_SMOKE_DATABASE_URL"
run = """
pnpm --filter @jant/core build:lib
node ./packages/core/dev/scripts/pg-smoke.mjs
"""

[tasks.check-types]
description = "Run TypeScript type checking (all packages)"
run = "pnpm -r typecheck"

[tasks.check-template]
description = "Integration test: scaffold a project with create-jant, then build + typecheck it"
run = """
pnpm --filter @jant/core build:lib
pnpm --filter create-jant prepublishOnly
pnpm --filter create-jant test-template
"""

# =============================================================================
# Lint / Format
# =============================================================================
[tasks.check-lint]
description = "Run ESLint"
run = "pnpm lint"

[tasks.fix-format]
description = "Format code with Prettier"
run = """
# Loop to a fixed point rather than formatting once. Prettier does not converge
# in a single pass on nested Lit `html` templates — each pass settles one more
# level of indentation, and the components under src/client/components have
# needed five. check-format compares against the fixed point, so formatting once
# can leave a tree that looks clean locally and still fails CI.
for pass in 1 2 3 4 5 6 7 8; do
  npx prettier --write . > /dev/null
  if npx prettier --check . > /dev/null 2>&1; then
    echo "Formatted. Converged after $pass pass(es)."
    exit 0
  fi
done

echo "Prettier did not converge after 8 passes. Still unformatted:" >&2
npx prettier --check .
exit 1
"""

[tasks.check-format]
description = "Check code formatting"
run = "npx prettier --check ."

[tasks.check-copy]
description = "Check user-facing copy against the writing style rules"
run = "node scripts/check-copy.mjs"

# =============================================================================
# CI (full gate)
# =============================================================================
[tasks.check-ci]
description = "Run all CI checks (format + lint + tests + i18n + template)"
run = """
# Phase 1: Static checks. Cheapest signal first, and CI has to be the real gate
# here: the pre-commit hook formats staged files but can fail to restage the
# result, which lands unformatted code without failing the commit.
mise run check-format
mise run check-lint

# Phase 2: Core checks
mise run check-tests
mise run check-copy
mise run i18n-check
if [[ -n "${PG_SMOKE_DATABASE_URL:-}" ]]; then
  mise run check-pg-smoke
fi

# Phase 3: Template integration
mise run check-template
"""

# =============================================================================
# DB — Schema codegen (SQLite/D1 + Postgres, must stay in sync)
# =============================================================================
[tasks.db-schema-generate]
description = "Generate SQLite/D1 Drizzle migrations from the current core schema"
run = "pnpm --filter @jant/core db:generate"

[tasks.db-schema-generate-pg]
description = "Generate PostgreSQL Drizzle migrations from the current core schema"
run = "pnpm --filter @jant/core db:generate:pg"

# =============================================================================
# DB — Local Wrangler (Miniflare + D1)
# =============================================================================
[tasks.db-wrangler-migrate]
description = "Run migrations for the local Wrangler/D1 development database"
dir = "packages/core"
run = "node ./bin/jant.js migrate"

[tasks.db-wrangler-clean]
alias = "clean"
description = "Delete the local Wrangler/D1 development database state (.wrangler)"
run = "rm -rf packages/core/.wrangler"

[tasks.db-wrangler-bootstrap-shell]
description = "Ensure the local Wrangler shell (admin, onboarding, default nav, debug creds)"
run = """
echo "Running local migrations..."
mise run db-wrangler-migrate
echo "Bootstrapping local development shell..."
mise run dev-auth-bootstrap
"""

[tasks.db-wrangler-load-demo]
description = "Load the canonical demo snapshot into the current local Wrangler/D1 database shell"
run = "node scripts/demo-local/import-canonical.mjs"

[tasks.db-wrangler-rebuild-demo]
description = "Recreate the local Wrangler/D1 database, bootstrap the local shell, and load the canonical demo snapshot"
dir = "packages/core"
run = """
echo "Resetting local database..."
rm -rf .wrangler
mise run db-wrangler-bootstrap-shell
echo "Loading canonical demo snapshot..."
node ../../scripts/demo-local/import-canonical.mjs
echo "Done! Local database is ready."
"""

[tasks.db-wrangler-rehearse]
description = "Rebuild a local Wrangler/D1 from the frozen migration rehearsal fixture"
dir = "packages/core"
run = "node ./bin/jant.js db rehearse --local --fixture src/db/rehearsal-fixtures/demo-current.json --persist-to .wrangler/rehearsal"

# =============================================================================
# DB — Local Node (SQLite via packages/core/.env.node)
# =============================================================================
[tasks.db-node-migrate]
description = "Run migrations for the local Node runtime database (reads .env.node)"
dir = "packages/core"
run = "node ./bin/jant.js migrate --node"

[tasks.db-node-clean]
description = "Delete the local Node runtime database and media storage"
run = """
echo "Removing local Node SQLite database and media..."
rm -rf packages/core/.data packages/core/.media
"""

[tasks.db-node-bootstrap-shell]
description = "Ensure the local Node runtime shell (admin, onboarding, default nav, debug creds)"
usage = 'arg "[password]" default="jant-dev-debug-login"'
run = """
echo "Running Node migrations..."
mise run db-node-migrate
echo "Bootstrapping local Node auth helpers..."
mise run dev-auth-bootstrap "$usage_password"
"""

[tasks.db-node-load-demo]
description = "Bootstrap the local Node runtime and import sites/demo-source/canonical/site-export into an empty local database"
usage = 'arg "[password]" default="jant-dev-debug-login"'
dir = "packages/core"
run = 'pnpm exec tsx dev/scripts/import-node-demo-site-export.ts "$usage_password"'

[tasks.db-node-rebuild-demo]
alias = "local-reset"
description = "Recreate the local Node SQLite database and media storage, bootstrap the local shell, and load the canonical demo snapshot"
usage = 'arg "[password]" default="jant-dev-debug-login"'
dir = "packages/core"
run = 'pnpm exec tsx dev/scripts/reset-node-dev.ts "$usage_password"'

# =============================================================================
# DB — Remote (Cloudflare D1)
# =============================================================================
[tasks.db-remote-migrate]
description = "Run migrations for the default remote D1 database"
dir = "packages/core"
run = "node ./bin/jant.js migrate --remote"

[tasks.db-remote-rehearse]
description = "Run the remote D1 migration rehearsal against the configured rehearsal database"
dir = "packages/core"
run = "node ./dev/scripts/run-remote-rehearsal.mjs"

# =============================================================================
# Site import (push exported content into a remote Jant instance)
# =============================================================================
[tasks.import]
description = "Import sites/demo-source/canonical/site-export into a remote Jant instance"
usage = 'arg "<url>"'
run = 'bash scripts/site/import.sh "$usage_url"'

[tasks.import-local]
description = "Import sites/demo-source/canonical/site-export into the local Node dev server (http://localhost:3000)"
run = 'bash scripts/site/import.sh "http://localhost:3000"'

# =============================================================================
# i18n (Lingui)
# =============================================================================
[tasks.i18n]
description = "Extract, translate with AI, and compile (requires OPENAI_API_KEY)"
run = "mise run i18n-extract && mise run i18n-translate && mise run i18n-compile"

[tasks.i18n-build]
description = "Extract and compile only (without translation)"
run = "pnpm --filter @jant/core i18n:build"

[tasks.i18n-check]
description = "Check i18n catalogs are in sync with source"
run = """
pnpm --filter @jant/core i18n:build
if ! git diff --exit-code packages/core/src/i18n/locales/; then
  echo ""
  echo "i18n catalogs are out of sync!"
  echo "Run 'mise run i18n-build' locally and commit the changes."
  exit 1
fi
"""

[tasks.i18n-extract]
description = "Extract messages from source"
run = "pnpm --filter @jant/core i18n:extract"

[tasks.i18n-compile]
description = "Compile PO files to JS"
run = "pnpm --filter @jant/core i18n:compile"

[tasks.i18n-translate]
description = "Translate settings catalog using AI (requires OPENAI_API_KEY)"
depends = ["i18n-translate-zh-Hans", "i18n-translate-zh-Hant"]

# Only the settings catalog is auto-translated. The public catalog is
# intentionally English-only — at runtime, non-English locales fall back to
# the source message for any key not in the settings catalog.
[tasks.i18n-translate-zh-Hans]
description = "Translate settings catalog to Simplified Chinese"
run = "bash scripts/i18n/translate-po.sh zh-Hans"

[tasks.i18n-translate-zh-Hant]
description = "Translate settings catalog to Traditional Chinese"
run = "bash scripts/i18n/translate-po.sh zh-Hant"

# =============================================================================
# Preview (deploy current branch for testing)
# =============================================================================
[tasks.deploy-preview]
description = "Build and deploy current branch to preview environment"
dir = "packages/core"
run = """
echo "Building @jant/core..."
pnpm run build
echo "Deploying to preview..."
node ./bin/jant.js deploy --env preview
"""

[tasks.db-preview-migrate]
description = "Run D1 migrations on preview environment"
dir = "packages/core"
run = "node ./bin/jant.js migrate --remote --env preview"

# =============================================================================
# Site: demo-source (private canonical source Worker)
# =============================================================================
[tasks.dev-demo-source]
description = "Start the private demo-source Worker (build @jant/core first)"
run = "pnpm --filter @jant/core build && pnpm --filter jant-demo-source dev"

[tasks.deploy-demo-source]
description = "Deploy the private demo-source Worker to Cloudflare Workers"
run = """
pnpm --filter @jant/core build
node scripts/demo-shared/run-with-env.mjs --site demo-source -- pnpm --filter jant-demo-source run deploy
"""

[tasks.db-demo-source-migrate]
description = "Run migrations on demo-source"
run = "node scripts/demo-shared/run-with-env.mjs --site demo-source --cwd sites/demo-source -- node ../../scripts/run-jant.mjs migrate --remote"

[tasks.demo-source-export-canonical]
description = "Refresh sites/demo-source/canonical/snapshot from the remote demo-source Worker"
run = "node scripts/demo-source/export-canonical.mjs"

[tasks.demo-source-export-canonical-site-export]
description = "Refresh sites/demo-source/canonical/site-export from the deployed demo-source site (requires JANT_API_TOKEN)"
run = "node scripts/demo-source/export-canonical-site-export.mjs"

[tasks.demo-source-clear-storage]
description = "Delete demo-source storage objects currently referenced by the remote runtime"
run = "node scripts/demo-source/clear-storage.mjs"

[tasks.demo-source-nuke]
description = "Delete referenced demo-source storage, then drop ALL tables in the demo-source database"
run = """
echo "Deleting currently referenced demo-source storage..."
node scripts/demo-source/clear-storage.mjs
echo "Dropping all demo-source database objects..."
node scripts/nuke-db.mjs sites/demo-source
"""

[tasks.demo-source-reset]
description = "Reset demo-source to an empty migrated schema"
run = """
mise run demo-source-nuke
echo "Reapplying demo-source migrations..."
mise run db-demo-source-migrate
echo "Done! demo-source is empty and migrated. Finish with /setup or site import."
"""

# =============================================================================
# Site: demo (public demo.jant.me)
# =============================================================================
[tasks.dev-demo]
description = "Start the public demo Worker locally (build @jant/core first)"
run = "pnpm --filter @jant/core build && pnpm --filter jant-demo dev"

[tasks.deploy-demo]
description = "Deploy the public demo site to demo.jant.me"
run = """
pnpm --filter @jant/core build
node scripts/demo-shared/run-with-env.mjs --site demo --cwd sites/demo -- pnpm exec jant deploy
"""

[tasks.db-demo-migrate]
description = "Run migrations on demo.jant.me"
run = "node scripts/demo-shared/run-with-env.mjs --site demo --cwd sites/demo -- node ../../scripts/run-jant.mjs migrate --remote"

[tasks.demo-nuke]
description = "Drop ALL tables in the public demo database (including migrations)"
run = "node scripts/nuke-db.mjs sites/demo"

[tasks.demo-bootstrap]
description = "Ensure demo.jant.me has its managed shell (admin, onboarding, default nav)"
run = """
echo "Running remote migrations..."
node scripts/demo-shared/run-with-env.mjs --site demo --cwd sites/demo -- node ../../scripts/run-jant.mjs migrate --remote
echo "Bootstrapping managed demo shell..."
node scripts/demo-shared/run-with-env.mjs --site demo --cwd sites/demo -- node scripts/bootstrap-demo.mjs
"""

[tasks.demo-clear-storage]
description = "Delete demo.jant.me storage objects currently referenced by the public runtime"
run = "node scripts/demo-public/clear-storage.mjs"

[tasks.demo-clear-content]
description = "Clear demo.jant.me content while preserving the managed shell"
run = """
mise run demo-bootstrap
echo "Deleting currently referenced demo storage..."
node scripts/demo-public/clear-storage.mjs
echo "Clearing demo content..."
node scripts/demo-public/clear-content.mjs
echo "Done! demo.jant.me content has been cleared."
"""

[tasks.demo-import-canonical]
description = "Import sites/demo-source/canonical/snapshot into demo.jant.me"
run = "node scripts/demo-public/import-canonical.mjs"

[tasks.demo-clear-api-tokens]
description = "Delete all user-created API tokens from demo.jant.me via the internal admin token"
run = "node scripts/demo-public/clear-api-tokens.mjs"

[tasks.demo-verify]
description = "Verify the public demo after a rebuild"
run = "node scripts/demo-public/verify.mjs"

[tasks.demo-rebuild]
description = "Restore demo.jant.me from sites/demo-source/canonical/snapshot"
run = """
node scripts/demo-public/import-canonical.mjs --check
node scripts/demo-public/clear-api-tokens.mjs --check
mise run demo-bootstrap
echo "Deleting DB-managed demo storage..."
node scripts/demo-public/clear-storage.mjs
echo "Importing canonical demo content..."
node scripts/demo-public/import-canonical.mjs
echo "Clearing demo API tokens..."
node scripts/demo-public/clear-api-tokens.mjs
mise run demo-verify
echo "Done! demo.jant.me has been rebuilt from the canonical snapshot."
"""

# =============================================================================
# Site: content-lab
# =============================================================================
[tasks.dev-content-lab]
description = "Start the content-lab Worker (build @jant/core first)"
run = "pnpm --filter @jant/core build && pnpm --filter jant-content-lab dev"

[tasks.deploy-content-lab]
description = "Deploy the content-lab Worker to Cloudflare Workers"
run = "pnpm --filter @jant/core build && pnpm --filter jant-content-lab run deploy"

[tasks.db-content-lab-migrate]
description = "Run migrations on content-lab.jant.me"
dir = "sites/content-lab"
run = "node ../../scripts/run-jant.mjs migrate --remote"

[tasks.db-content-lab-export]
description = "Export content-lab content to scripts/content-lab-snapshot.sql"
dir = "sites/content-lab"
run = "node scripts/export-content-lab.mjs"

[tasks.db-content-lab-clean]
description = "Clear content-lab content without touching users or settings"
dir = "sites/content-lab"
run = """
echo "Clearing content-lab data..."
node scripts/clear-content.mjs
echo "Done! content-lab content has been cleared."
"""

[tasks.db-content-lab-nuke]
description = "Drop ALL tables in content-lab database (including migrations)"
run = "node scripts/nuke-db.mjs sites/content-lab"

# =============================================================================
# Docker
# =============================================================================
[tasks.docker-build]
description = "Build the local Docker image"
usage = 'arg "[image]" default="jant:local"'
run = 'docker build -t "$usage_image" .'

[tasks.docker-build-pull]
description = "Build the local Docker image and refresh base layers"
usage = 'arg "[image]" default="jant:local"'
run = 'docker build --pull -t "$usage_image" .'

[tasks.docker-pull]
description = "Pull the published image used by compose.yml"
run = "docker compose pull"

[tasks.docker-push]
description = "Push a tagged Docker image to its registry"
usage = 'arg "<image>"'
run = 'docker push "$usage_image"'

[tasks.docker-dev-build]
description = "Build the local development Docker Compose image"
run = "docker compose -f compose.dev.yml build"

[tasks.docker-dev-up]
description = "Start the development Docker Compose stack from the local checkout"
run = "docker compose -f compose.dev.yml up --build"

[tasks.docker-dev-down]
description = "Stop the development Docker Compose stack"
run = "docker compose -f compose.dev.yml down"

[tasks.docker-dev-logs]
description = "Tail logs from the development Docker Compose stack"
run = "docker compose -f compose.dev.yml logs -f"

[tasks.docker-up]
description = "Start the bundled Docker Compose stack"
run = "docker compose up -d"

[tasks.docker-up-local]
description = "Start the bundled Docker Compose stack with a local image tag"
usage = 'arg "[image]" default="jant:local"'
run = 'IMAGE="$usage_image" docker compose up -d'

[tasks.docker-down]
description = "Stop the bundled Docker Compose stack"
run = "docker compose down"

[tasks.docker-logs]
description = "Tail logs from the bundled Docker Compose stack"
run = "docker compose logs -f"

# =============================================================================
# Worktree (git worktree workflow)
# =============================================================================
[tasks.draft]
description = "Create a new feature worktree: mise run draft feat/login [base-branch]"
usage = """
arg "<name>"
arg "[base]" default="main"
"""
run = 'bash scripts/worktree/draft.sh "$usage_name" "$usage_base"'

[tasks.worktree-review]
description = "Create a review worktree from a remote branch: mise run worktree-review feat/login"
usage = 'arg "<branch>"'
run = 'bash scripts/worktree/review.sh "$usage_branch"'

[tasks.worktree-remove]
description = "Remove a worktree and its branch: mise run worktree-remove feat/login"
usage = 'arg "<branch>"'
run = 'bash scripts/worktree/remove.sh "$usage_branch"'

[tasks.worktree-remove-all]
description = "Remove all worktrees except the current one and delete their branches"
run = "bash scripts/worktree/remove-all.sh"

[tasks.worktree-list]
description = "List all git worktrees"
run = "git worktree list"

[tasks.worktree-sync]
alias = "ws"
description = "Merge the primary branch into worker worktrees and report targets that fail"
run = "bash scripts/worktree/sync.sh"

[tasks.worktree-sync-check]
alias = "wsc"
description = "Check worker worktrees for commits not contained in preview"
run = "bash scripts/worktree/sync-check.sh"

[tasks.branch-remove-all-local]
description = "Delete all local branches except main"
run = "git branch | grep -v '^\\* main$' | grep -v '^  main$' | xargs git branch -D"

# =============================================================================
# Examples (scaffolded via create-jant from sites/demo)
# =============================================================================
[tasks.example-create]
description = "Create a new example from sites/demo via create-jant"
usage = 'arg "<name>"'
run = 'bash scripts/example/create.sh "$usage_name"'

[tasks.example-run]
description = "Run a script in an example project: mise run example-run <name> [script]"
usage = """
arg "<name>"
arg "[script]" default="dev"
"""
run = 'pnpm --filter "./examples/$usage_name" "$usage_script"'

# =============================================================================
# Starter template
# =============================================================================
[tasks.starter-generate]
description = "Generate jant-starter template using create-jant CLI (for testing sync-starter workflow)"
run = "bash scripts/starter/generate.sh"

# =============================================================================
# Code generation
# =============================================================================
[tasks.codegen-icons]
description = "Generate ALL_ICON_NAMES in icon-catalog.ts from lucide-static"
run = "npx tsx scripts/generate-icon-catalog.ts"

# =============================================================================
# Release & Publishing (Changesets)
# =============================================================================
[tasks.changeset]
description = "Create a new changeset for your changes"
run = "pnpm changeset"

[tasks.changeset-add]
description = "Create a changeset non-interactively: mise run changeset-add <patch|minor|major> <summary>"
usage = """
arg "<bump>"
arg "<summary>"
"""
run = 'node scripts/release/changeset-add.mjs "$usage_bump" "$usage_summary"'

[tasks.changeset-status]
description = "Show pending changesets and version bumps"
run = "pnpm changeset status"

[tasks.release-version]
description = "Apply changesets and bump versions (generates CHANGELOG)"
run = "pnpm changeset version"

[tasks.release-publish]
description = "Build and publish all packages to npm (CI only, requires provenance)"
run = "pnpm release"

[tasks.release-publish-local]
description = "Build and publish all packages locally (packages/* + themes/*)"
run = "bash scripts/release/publish-local.sh"

[tasks.release-publish-dry]
description = "Dry run of release (no actual publish)"
run = "pnpm --filter @jant/core build && pnpm -r --filter './packages/*' --filter './themes/*' publish --dry-run --no-provenance"

[tasks.release-publish-core]
description = "Build and publish @jant/core to npm"
run = "pnpm --filter @jant/core build && cd packages/core && npm publish --access public --no-provenance --no-git-checks"

[tasks.release-publish-create]
description = "Build and publish create-jant to npm"
run = "pnpm --filter create-jant build && cd packages/create-jant && npm publish --access public --no-provenance --no-git-checks"

# =============================================================================
# Utilities
# =============================================================================
[tasks.auth-reset-token]
description = "Generate a password reset token for the current local runtime"
dir = "packages/core"
run = "bash -lc 'set -a; if [ -f .env.node ]; then source .env.node; fi; set +a; npx jant reset-password'"

[tasks.clean-build]
description = "Clean build artifacts"
run = "rm -rf packages/*/dist themes/*/dist packages/*/.wrangler templates/*/.wrangler examples/"

[tasks.clean-deps]
description = "Remove all node_modules and reinstall"
run = "rm -rf node_modules packages/*/node_modules templates/*/node_modules themes/*/node_modules examples/ && pnpm install"

[tasks.clean-reset]
description = "Nuclear reset - delete everything and start fresh (node_modules, dist, cache, db, lock file)"
run = "bash scripts/clean/reset.sh"
